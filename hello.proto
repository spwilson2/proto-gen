syntax = "proto3";

service hi {
    rpc do(something) returns (null);

}

message searchrequest {
  string query = 1;
  int32 page_number = 2;
  int32 results_per_page = 3;
  message two {
    bool j = 1;
    int32 d = 2;
  }
  optional two me = 4;
}